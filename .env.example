# Cloudflare Tunnel Token（见 .skills/部署流程.md 3.4 节获取）
CLOUDFLARE_TUNNEL_TOKEN=

# X (Twitter) auth_token cookie value
# 获取方式：浏览器登录 x.com → F12 → Application → Cookies → auth_token
# 多 token 逗号分隔，自动轮询负载均衡
TWITTER_AUTH_TOKEN=
